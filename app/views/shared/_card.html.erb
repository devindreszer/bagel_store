<div class="card">
  <div class="card-image"><%= image_tag(menu_item.image_url, alt: menu_item.name) %></div>
  <div class="card-name"><h3><%= menu_item.name %></h3></div>
  <div class="card-price"><p><%= number_to_currency(menu_item.price) %></p></div>
</div>
